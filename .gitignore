# Ambiente Python
.venv/
__pycache__/
*.pyc

# Arquivos temporários de renderização
_p*.png
_*.txt

# Artefato gerado — NÃO versionar (reconstruído por gerar_site.py no CI)
site/

# Editais (fonte de referência) — manter localmente, fora do versionamento
*.pdf

# macOS
.DS_Store

# Material do canal (TTS + descrições) — NÃO versionar
youtube/

# Provas (referência local) — NÃO versionar
/provas/
/prova sefaz go/
gerar_pdf_prova*.html
/gabaritos/
